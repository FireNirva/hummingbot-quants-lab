# 🔄 Base 链套利池筛选指南

## 📋 配置文件说明

配置文件: `config/base_arbitrage_pools_screener.yml`

这个配置包含 **5 个不同的筛选策略**，针对 Base 链上的不同套利场景。

---

## 🎯 五大套利策略

### 1️⃣ 高流动性稳定套利 (`base_high_liquidity_arb`)

**特点**:
- ✅ 大流动性（20万美元+）
- ✅ 稳定交易量（30万美元+/天）
- ✅ 低滑点风险
- ✅ 适合大资金量

**参数配置**:
```yaml
min_pool_age_days: 7          # 7天历史
min_fdv: 500000               # 50万美元
min_volume_24h: 300000        # 30万美元日交易量
min_liquidity: 200000         # 20万美元流动性
min_transactions_24h: 500     # 500笔交易
```

**适合场景**: 
- 大额套利（>$10,000）
- 稳定收益目标（1-3% per trade）
- 风险厌恶型

**扫描频率**: 每 30 分钟

---

### 2️⃣ 高交易量热门套利 (`base_hot_volume_arb`)

**特点**:
- 🔥 极高交易量（50万美元+/天）
- 🔥 快速资金轮转
- 🔥 频繁套利机会
- 🔥 适合高频交易

**参数配置**:
```yaml
min_pool_age_days: 3          # 3天历史
min_fdv: 200000               # 20万美元
min_volume_24h: 500000        # 50万美元（关键！）
min_liquidity: 100000         # 10万美元
min_transactions_24h: 800     # 800笔高频交易
```

**适合场景**:
- 中小额高频套利（$1,000-$5,000）
- 快进快出（持仓 <30 分钟）
- 积极套利策略

**扫描频率**: 每 15 分钟（最频繁）

---

### 3️⃣ ETH 配对跨链套利 (`base_eth_pair_arb`)

**特点**:
- 🌉 ETH 作为报价资产
- 🌉 可与其他链对比
- 🌉 跨链套利机会
- 🌉 适合跨 DEX 套利

**参数配置**:
```yaml
quote_asset: "ETH"            # 关注 ETH 配对
min_pool_age_days: 5
min_volume_24h: 200000        # 20万美元
min_liquidity: 150000         # 15万美元
```

**适合场景**:
- Base vs Ethereum 主网价差
- Base vs Arbitrum/Optimism 套利
- ETH 相对价值套利

**扫描频率**: 每 30 分钟

---

### 4️⃣ 早期新池套利 (`base_new_pools_arb`)

**特点**:
- 🆕 新建池子（1-3天）
- 🆕 早期机会
- ⚠️ 较高风险
- 🆕 波动性大

**参数配置**:
```yaml
min_pool_age_days: 1          # 1天新池
min_fdv: 100000               # 10万美元（较低）
min_volume_24h: 100000        # 10万美元
min_liquidity: 50000          # 5万美元（风险！）
```

**适合场景**:
- 小额测试（<$1,000）
- 高风险高收益
- 快速止损策略

**风险提示**: ⚠️ 新池子可能存在 rug pull、低流动性、高滑点等风险

**扫描频率**: 每 30 分钟

---

### 5️⃣ 均衡中等规模套利 (`base_balanced_arb`)

**特点**:
- ⚖️ 平衡的参数设置
- ⚖️ 中等风险收益
- ⚖️ 适合大多数场景
- ⚖️ 通用策略

**参数配置**:
```yaml
min_pool_age_days: 5          # 5天
min_fdv: 250000               # 25万美元
min_volume_24h: 150000        # 15万美元
min_liquidity: 100000         # 10万美元
```

**适合场景**:
- 通用套利策略
- 中等资金量（$2,000-$8,000）
- 风险适中

**扫描频率**: 每 1 小时

---

## 🚀 运行命令

### 方法 1: 运行所有策略（推荐）

```bash
cd /Users/alice/Dropbox/投资/量化交易/quants-lab

# 持续运行，按配置的时间间隔自动执行
python cli.py run-tasks --config base_arbitrage_pools_screener.yml
```

这会同时运行所有 5 个策略，各自按自己的时间表执行。

---

### 方法 2: 单独运行某个策略

```bash
# 只运行高流动性策略
python cli.py trigger-task \
  --task base_high_liquidity_arb \
  --config base_arbitrage_pools_screener.yml

# 只运行热门交易量策略
python cli.py trigger-task \
  --task base_hot_volume_arb \
  --config base_arbitrage_pools_screener.yml

# 只运行 ETH 配对策略
python cli.py trigger-task \
  --task base_eth_pair_arb \
  --config base_arbitrage_pools_screener.yml

# 只运行新池子策略
python cli.py trigger-task \
  --task base_new_pools_arb \
  --config base_arbitrage_pools_screener.yml

# 只运行均衡策略
python cli.py trigger-task \
  --task base_balanced_arb \
  --config base_arbitrage_pools_screener.yml
```

---

### 方法 3: 后台持续运行

```bash
# 在后台运行，并记录日志
nohup python cli.py run-tasks \
  --config base_arbitrage_pools_screener.yml \
  > logs/base_arbitrage.log 2>&1 &

# 查看日志
tail -f logs/base_arbitrage.log
```

---

### 方法 4: 验证配置（运行前检查）

```bash
# 验证配置文件是否正确
python cli.py validate-config --config base_arbitrage_pools_screener.yml

# 列出所有任务
python cli.py list-tasks --config base_arbitrage_pools_screener.yml
```

---

## 📊 查看结果

### 在 MongoDB 中查看

数据存储位置:
- **数据库**: `quants_lab`
- **集合**: `pools`

### 使用 Jupyter Notebook

```python
import pandas as pd
from core.database_manager import db_manager
import asyncio

async def analyze_base_pools():
    # 获取 MongoDB 客户端
    mongo = await db_manager.get_mongodb_client()
    
    # 查询 Base 链最新的筛选结果
    results = await mongo.find_documents(
        collection_name="pools",
        query={"network": "base"},
        sort=[("timestamp", -1)],
        limit=1
    )
    
    if not results:
        print("还没有数据，请先运行任务")
        return
    
    latest = results[0]
    print(f"📅 数据时间: {latest['timestamp']}")
    print(f"🌐 网络: {latest.get('network', 'N/A')}")
    
    # 分析筛选后的池子
    filtered_pools = latest.get('filtered_trending_pools', [])
    
    if filtered_pools:
        df = pd.DataFrame(filtered_pools)
        
        # 计算套利潜力分数
        df['arb_score'] = df['volume_liquidity_ratio']
        
        # 按套利潜力排序
        top_arb = df.nlargest(10, 'arb_score')
        
        print(f"\n🔝 Top 10 套利潜力池子:")
        print(top_arb[[
            'name', 
            'volume_usd_h24', 
            'reserve_in_usd', 
            'volume_liquidity_ratio',
            'transactions_h24_buys',
            'transactions_h24_sells'
        ]])
        
        return top_arb
    else:
        print("没有找到符合条件的池子")

# 运行分析
result = await analyze_base_pools()
```

---

### 使用 Mongo Express（Web UI）

1. 启动数据库:
```bash
make run-db
```

2. 访问: http://localhost:28081/
   - 用户名: `admin`
   - 密码: `changeme`

3. 导航到: `quants_lab` → `pools`

---

## 📈 套利指标分析

### 关键指标理解

#### 1. `volume_liquidity_ratio` (交易量/流动性比率)

```python
ratio = volume_24h / liquidity
```

**含义**:
- **> 3.0**: 🔥 资金周转极快，套利机会多
- **1.5 - 3.0**: ✅ 活跃池子，适合套利
- **< 1.5**: ⚠️ 相对不活跃

**套利策略**:
- 高比率池子适合高频套利
- 关注比率突增的池子（可能有大新闻）

---

#### 2. `fdv_liquidity_ratio` (FDV/流动性比率)

```python
ratio = fdv / liquidity
```

**含义**:
- **< 20**: 💎 流动性充足，滑点小
- **20 - 50**: ✅ 正常范围
- **> 50**: ⚠️ 流动性不足，滑点风险

**套利策略**:
- 低比率池子适合大额套利
- 高比率池子需要小额测试

---

#### 3. `price_change_percentage_h1` (1小时价格变化)

**含义**:
- **> 5%**: 🔥 高波动，套利机会
- **2% - 5%**: ✅ 正常波动
- **< 2%**: 相对稳定

**套利策略**:
- 价格快速变化时，检查其他 DEX 的价差
- 波动率高 = 套利空间大

---

#### 4. 交易笔数比 (`buys / sells`)

```python
buy_sell_ratio = transactions_h24_buys / transactions_h24_sells
```

**含义**:
- **> 1.2**: 📈 买盘强劲
- **0.8 - 1.2**: ⚖️ 平衡
- **< 0.8**: 📉 卖压较大

**套利策略**:
- 失衡时可能存在套利机会
- 结合价格走势判断方向

---

## 🎯 套利策略示例

### 场景 1: 稳健套利

**目标**: 稳定收益，低风险

**策略**:
1. 使用 `base_high_liquidity_arb` 筛选池子
2. 筛选条件:
   - `volume_liquidity_ratio > 1.5`
   - `fdv_liquidity_ratio < 30`
   - `liquidity > $200,000`
3. 对比其他 DEX（Uniswap、SushiSwap）价格
4. 价差 > 0.5% 时执行套利
5. 单笔金额: $10,000 - $50,000

**预期收益**: 0.3% - 1% per trade

---

### 场景 2: 高频套利

**目标**: 频繁交易，累积收益

**策略**:
1. 使用 `base_hot_volume_arb` 筛选池子
2. 实时监控（15分钟一次）
3. 筛选条件:
   - `volume_liquidity_ratio > 3.0`
   - `transactions_h24 > 800`
4. 快速执行（<30秒）
5. 单笔金额: $1,000 - $5,000

**预期收益**: 0.2% - 0.8% per trade，高频率

---

### 场景 3: 跨链套利

**目标**: 利用 Base 与其他链的价差

**策略**:
1. 使用 `base_eth_pair_arb` 筛选池子
2. 对比价格:
   - Base (Aerodrome, BaseSwap)
   - Ethereum (Uniswap)
   - Arbitrum (Camelot)
3. 计算跨链成本（桥接费用）
4. 净收益 > 0.8% 时执行

**预期收益**: 0.5% - 2% per trade

---

### 场景 4: 新池早期套利

**目标**: 捕捉新池子的早期机会

**策略**:
1. 使用 `base_new_pools_arb` 筛选
2. 快速评估:
   - 项目合约是否安全
   - 团队是否可信
   - 流动性是否锁定
3. 小额测试（$100-$500）
4. 发现异常立即撤出

**风险**: ⚠️⚠️⚠️ 高风险！可能遇到 rug pull

**预期收益**: 高收益潜力（5%-20%），但风险大

---

## ⚠️ 风险提示

### 常见风险

1. **滑点风险**
   - 流动性不足导致实际价格偏离
   - 解决: 限制单笔金额，使用限价单

2. **Gas 费用**
   - Base 链 Gas 费较低，但需计入成本
   - 解决: 确保净收益 > Gas 费

3. **时间风险**
   - 价差消失速度快
   - 解决: 使用快速执行工具，MEV 保护

4. **智能合约风险**
   - 新池子可能有漏洞或后门
   - 解决: 审计合约，小额测试

5. **流动性枯竭**
   - 大额交易后流动性不足
   - 解决: 分批执行，监控深度

---

## 🛠️ 故障排查

### 问题 1: 任务执行失败

```bash
# 检查配置
python cli.py validate-config --config base_arbitrage_pools_screener.yml

# 查看详细错误
python cli.py trigger-task --task base_high_liquidity_arb \
  --config base_arbitrage_pools_screener.yml
```

### 问题 2: 没有找到池子

**可能原因**:
- 参数设置过严格
- Base 链数据未更新
- GeckoTerminal API 限制

**解决方案**:
1. 放宽参数（降低 min_volume, min_liquidity）
2. 检查网络连接
3. 等待一段时间后重试

### 问题 3: MongoDB 连接失败

```bash
# 启动数据库
make run-db

# 检查状态
docker ps | grep mongodb

# 查看日志
docker logs mongodb
```

---

## 📚 进阶优化

### 优化 1: 添加价格监控

创建一个脚本持续监控池子价格变化:

```python
# monitor_base_arb.py
import asyncio
from core.database_manager import db_manager

async def monitor_prices():
    mongo = await db_manager.get_mongodb_client()
    
    while True:
        results = await mongo.find_documents(
            "pools",
            {"network": "base"},
            sort=[("timestamp", -1)],
            limit=1
        )
        
        # 处理数据，检测套利机会
        # ...
        
        await asyncio.sleep(60)  # 每分钟检查

asyncio.run(monitor_prices())
```

### 优化 2: 多链对比

同时运行多个网络的筛选器，对比价格。

### 优化 3: 自动执行

将筛选结果连接到交易执行引擎（需要额外开发）。

---

## 🎓 总结

**配置文件**: `config/base_arbitrage_pools_screener.yml`

**5 大策略**:
1. 🏦 高流动性稳定套利 - 大额稳健
2. 🔥 高交易量热门套利 - 高频交易
3. 🌉 ETH 配对跨链套利 - 跨链机会
4. 🆕 早期新池套利 - 高风险高收益
5. ⚖️ 均衡中等规模套利 - 通用策略

**开始使用**:
```bash
python cli.py run-tasks --config base_arbitrage_pools_screener.yml
```

**祝你套利愉快！💰✨**

---

*最后更新: 2024-10-04*

